// A one-function library, included by `squares.fxc`.
//
// A library is a set of `fn` definitions (and optionally `const` values) with
// no `fn main()` of its own. It defines nothing until another program calls it,
// and it is inlined at each call, so a call costs exactly its body.

fn increment(x) = x + 1;
