node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# The WebAssembly module, built from the Rust crates by `bun run build:wasm`.
# Generated, so it is not committed: CI builds it, and locally it is built once
# and then left alone while the frontend is iterated on.
/static/fx_wasm.wasm

# Generated from `../examples/*.fxc` by `bun run build:examples`.
/src/lib/examples.generated.ts
